# Environment Variables Template
# Copy this file to .env and fill in values

# ---- LLM API Keys (optional — only needed for LLM-based query parsing/reranking) ----
OPENAI_API_KEY=your-openai-api-key-here
#OPENROUTER_API_KEY=
# GOOGLE_API_KEY=your-google-api-key-here

# ---- Embedding Model Selection ----
# Options:
#   "sentence-transformers/all-MiniLM-L6-v2"     (80MB, fast, general)
#   "medicalai/ClinicalBERT"                      (400MB, clinical)
#   "emilyalsentzer/Bio_ClinicalBERT"             (700MB, best clinical)
#   "openai"                                       (API call, best quality)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ---- Search Configuration ----
DEFAULT_TOP_K=10
DEFAULT_APPROACH=hybrid          # Options: boolean, semantic, hybrid
DEFAULT_NEGATION_MODE=clinical_concept   # Options: literal, clinical_concept

# ---- Hybrid Score Weights ----
BM25_WEIGHT=0.40
VECTOR_WEIGHT=0.40
BOOLEAN_WEIGHT=0.20

# ---- Index Paths ----
BM25_INDEX_PATH=indexing/index_store/bm25
FAISS_INDEX_PATH=indexing/index_store/faiss

# ---- Data Paths ----
NOTES_DATA_PATH=data/raw/clinical_notes
RESEARCH_DATA_PATH=data/raw/research_abstracts

# ---- API Settings ----
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=true

# ---- Corpus Generation ----
NUM_CLINICAL_NOTES=1500
NUM_RESEARCH_ABSTRACTS=200
RANDOM_SEED=42
